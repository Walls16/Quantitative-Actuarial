# Quantitative Actuarial

Pure actuarial and quantitative-finance routines packaged as
`quantitativeactuarial`, with a Streamlit consumer app under `app/`.

```python
import quantitativeactuarial as quact

engine = quact.FinancialMathEngine()
engine.tasa_nominal_a_efectiva(0.12, 12)
```
